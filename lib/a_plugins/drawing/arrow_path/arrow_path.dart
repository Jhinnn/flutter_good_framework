
import 'package:flutter/material.dart';

class ArrowPathDemo extends StatelessWidget {
  const ArrowPathDemo({Key? key}) : super(key: key);

  Widget build(BuildContext context) {
 
    return Scaffold(
      appBar: AppBar(title: const Text('ArrowPathDemo')),
      body: SingleChildScrollView(
        child: Column(
          children:const[
         
          ],
        ),
      ),
    );
  }
}
