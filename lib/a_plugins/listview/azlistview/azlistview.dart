import 'dart:io';

import 'package:flutter/material.dart';

import '../../../z_widget/div_scaffold.dart';

class AListViewDemo extends StatelessWidget {
  const AListViewDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const LRScaffold(
        title: 'AzListView',
        child: Center(
          child: Text('/Users/felix/Desktop/plugins/listview'),
        ));
  }
}
